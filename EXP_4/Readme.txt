Compile and run "Source.cpp" for frequency filtering

Keep all the input images in the same folder as "Source.cpp"

Input:Move trackbar to select input image, filter type and cutoff.
Output: Filtered image.

Check console output for selected input image, filter type and output image

key points:
1. Cutoff trackber is hardcoded to start from 1. please dont move it back to 0.
